#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

	int ahpla;

	for (ahpla = 122; ahpla >= 97; ahpla--)
	{
		putchar(ahpla);
	}
	putchar(10);
	return (0);
}
